---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components.json
  - lib/utils.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev boots without errors and http://localhost:3000 returns 200"
    - "npm run build completes without TypeScript or lint errors"
    - "shadcn/ui Button and Card components render on the root page"
    - "Tailwind custom tokens (warm beige palette, Quicksand + Nunito fonts) are active"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js 14 App Router config with image domain allowlist"
    - path: "tailwind.config.ts"
      provides: "Extended Tailwind theme with KAOKAO design tokens"
    - path: "app/layout.tsx"
      provides: "Root layout with Quicksand + Nunito fonts and globals.css import"
    - path: "app/globals.css"
      provides: "Tailwind base directives + CSS variables for the warm beige palette"
    - path: "components.json"
      provides: "shadcn/ui configuration pointing to lib/utils.ts"
    - path: "lib/utils.ts"
      provides: "cn() helper from clsx + tailwind-merge"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import"
      pattern: "import.*globals.css"
    - from: "app/layout.tsx"
      to: "next/font/google"
      via: "Quicksand and Nunito font imports"
      pattern: "Quicksand|Nunito"
---

<objective>
Scaffold the Next.js 14 App Router project, wire Tailwind CSS with KAOKAO design tokens,
and initialize shadcn/ui so the dev server boots and the build pipeline is clean.

Purpose: All subsequent phases require a working framework with the correct styling system.
Output: A green dev server, passing build, and a root page that renders shadcn/ui components
with the warm beige palette.
</objective>

<execution_context>
@/home/tanyong47/.claude/get-shit-done/workflows/execute-plan.md
@/home/tanyong47/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@spec.md
@DSprompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 App Router project with TypeScript</name>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.mjs, .gitignore</files>
  <read_first>
    - /mnt/c/KaoProJ/kaokao/CLAUDE.md (stack decisions — Next.js 14, exact package names)
    - /mnt/c/KaoProJ/kaokao/.gitignore (must include .env.local; verify it is present)
    - /mnt/c/KaoProJ/kaokao/package.json (check if Next.js is already installed)
  </read_first>
  <action>
    Run: npx create-next-app@14 . --typescript --app --no-src-dir --no-tailwind --no-eslint --import-alias "@/*" --use-npm

    If the project already has a package.json with next listed, skip create-next-app and proceed directly to installing missing packages.

    After scaffolding, install exact versions: next@14, react@18, react-dom@18, typescript@5, @types/node, @types/react, @types/react-dom.

    Set next.config.ts to export a NextConfig object with:
    - images.remotePatterns: allow hostname matching *.supabase.co (protocol https, port empty, pathname /**)
    - experimental.serverComponentsExternalPackages: [] (empty for now; sharp will be added in Phase 3)
    - Do NOT add i18n — out of scope per spec

    Confirm .gitignore contains .env.local (it must already from the repo init — verify, do not overwrite the file if it exists, only append the line if missing).
  </action>
  <verify>
    <automated>cd /mnt/c/KaoProJ/kaokao && node -e "const p=require('./package.json'); console.log('next:', p.dependencies.next)" 2>/dev/null || npx next --version</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists next@14.x, react@18.x as dependencies
    - next.config.ts exports default NextConfig with images.remotePatterns containing supabase.co
    - .gitignore contains the line ".env.local"
    - tsconfig.json has "paths": { "@/*": ["./*"] } so @/lib/utils resolves correctly
  </acceptance_criteria>
  <done>next.config.ts exists with supabase.co remote pattern; package.json has next@14; .gitignore covers .env.local</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind CSS with KAOKAO design tokens and initialize shadcn/ui</name>
  <files>tailwind.config.ts, postcss.config.mjs, app/globals.css, components.json, lib/utils.ts, app/layout.tsx, app/page.tsx</files>
  <read_first>
    - /mnt/c/KaoProJ/kaokao/DSprompt.md (exact color palette, typography choices — Quicksand + Nunito, spacing, border radius)
    - /mnt/c/KaoProJ/kaokao/CLAUDE.md (shadcn/ui and Tailwind are the confirmed stack)
    - /mnt/c/KaoProJ/kaokao/spec.md section 6 (non-functional: responsive mobile-first, 375px breakpoint)
  </read_first>
  <action>
    Install: tailwindcss@3, postcss@8, autoprefixer, clsx, tailwind-merge, class-variance-authority, lucide-react.

    Run shadcn/ui init: npx shadcn-ui@latest init --defaults to generate components.json.
    If the interactive prompt cannot be suppressed, set components.json manually with: style=default, rsc=true, tsx=true, tailwind.config=tailwind.config.ts, tailwind.css=app/globals.css, aliases.components=@/components, aliases.utils=@/lib/utils.

    Add shadcn components needed for the root page placeholder: npx shadcn-ui@latest add button card.

    Configure tailwind.config.ts with:
    - content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}", "./lib/**/*.{ts,tsx}"]
    - theme.extend.colors: kaokao palette — beige: #E8DCCB, cream: #F7F3EE, brown: #A68A64, sage: #A8C3A0, orange: #F4B183, text-dark: #3D3D3D
    - theme.extend.fontFamily: sans maps to Quicksand (primary), display maps to Nunito
    - theme.extend.screens: add "xs": "375px" as the base mobile breakpoint

    In app/globals.css: add @tailwind base/components/utilities directives; set CSS variables for shadcn/ui color tokens aligned to the beige palette (--background: warm beige, --foreground: dark text #3D3D3D, --primary: soft brown #A68A64, --radius: 0.75rem for rounded corners).

    In app/layout.tsx: import Quicksand and Nunito from next/font/google. Apply both as CSS variables on the html element. Import globals.css. Set metadata.title = "KAOKAO" and metadata.description = "Find lost pets near you".

    In app/page.tsx: render a minimal placeholder using shadcn Button and Card — enough to confirm shadcn/ui is wired. Include the text "KAOKAO" in an h1 with the Nunito font class. This page will be replaced in Phase 2.

    In lib/utils.ts: export cn() using clsx and twMerge.
  </action>
  <verify>
    <automated>cd /mnt/c/KaoProJ/kaokao && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 with no TypeScript or ESLint errors
    - tailwind.config.ts has theme.extend.colors containing keys: beige, cream, brown, sage, orange
    - tailwind.config.ts content array includes "./app/**/*.{ts,tsx}" and "./components/**/*.{ts,tsx}"
    - app/globals.css contains "@tailwind base", "@tailwind components", "@tailwind utilities"
    - app/layout.tsx imports both Quicksand and Nunito from "next/font/google"
    - lib/utils.ts exports a function named cn
    - components.json exists at project root with style field set
    - components/ui/button.tsx exists (shadcn added successfully)
    - npm run dev starts without error; curl http://localhost:3000 returns 200 when dev server is running
  </acceptance_criteria>
  <done>npm run build exits 0; tailwind design tokens are wired; shadcn/ui Button and Card render on root page; fonts load via next/font</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party packages installed from public registry |
| Developer machine → git repo | .env.local must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local / secrets | mitigate | Confirm .gitignore contains .env.local before first commit; enforce via pre-commit hook in Phase 2 |
| T-01-02 | Tampering | npm packages (create-next-app, shadcn-ui, tailwindcss) | mitigate | All packages are well-known with >1M weekly downloads; verify via npmjs.com before install; lock with package-lock.json |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck — verify all installed packages are legitimate (create-next-app, shadcn-ui@latest, tailwindcss, clsx, tailwind-merge, lucide-react) against npmjs.com before running install |
</threat_model>

<verification>
1. npm run dev boots: `npm run dev &amp; sleep 5 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000`  — expect 200
2. npm run build passes: `npm run build` exits 0
3. Tailwind tokens present: `grep -v '^//' tailwind.config.ts | grep "beige\|cream\|brown\|sage"` — returns matches
4. shadcn/ui wired: `test -f components/ui/button.tsx &amp;&amp; echo OK`
5. .env.local gitignored: `grep -F ".env.local" .gitignore` — returns match
</verification>

<success_criteria>
- npm run dev boots and http://localhost:3000 returns 200
- npm run build exits 0 with no errors
- KAOKAO palette colors defined in tailwind.config.ts
- Quicksand + Nunito fonts loaded via next/font/google in layout.tsx
- shadcn/ui Button and Card components installed and render on root page
- .env.local is in .gitignore
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
