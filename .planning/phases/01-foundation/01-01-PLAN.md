---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components.json
  - lib/utils.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev boots without errors and http://localhost:3000 returns 200"
    - "npm run build completes without TypeScript or lint errors"
    - "npm run lint exits 0 (ESLint wired by create-next-app)"
    - "shadcn/ui Button and Card components render on the root page"
    - "Tailwind custom tokens (warm beige palette, Quicksand + Nunito fonts) are active"
  artifacts:
    - path: "next.config.mjs"
      provides: "Next.js 14 App Router config with image domain allowlist for Supabase"
    - path: "tailwind.config.ts"
      provides: "Extended Tailwind theme with KAOKAO design tokens"
    - path: "app/layout.tsx"
      provides: "Root layout with Quicksand + Nunito fonts and globals.css import"
    - path: "app/globals.css"
      provides: "Tailwind base directives + CSS variables for the warm beige palette"
    - path: "components.json"
      provides: "shadcn/ui configuration pointing to lib/utils.ts"
    - path: "lib/utils.ts"
      provides: "cn() helper from clsx + tailwind-merge"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import"
      pattern: "import.*globals.css"
    - from: "app/layout.tsx"
      to: "next/font/google"
      via: "Quicksand and Nunito font imports"
      pattern: "Quicksand|Nunito"
---

<objective>
Scaffold the Next.js 14 App Router project, wire Tailwind CSS with KAOKAO design tokens,
and initialize shadcn/ui so the dev server boots and the build pipeline is clean.

Purpose: All subsequent phases require a working framework with the correct styling system.
Output: A green dev server, passing build, and a root page that renders shadcn/ui components
with the warm beige palette.
</objective>

<execution_context>
@/home/tanyong47/.claude/get-shit-done/workflows/execute-plan.md
@/home/tanyong47/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@spec.md
@DSprompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 14 App Router project with TypeScript and ESLint</name>
  <files>package.json, next.config.mjs, tsconfig.json, postcss.config.mjs, .gitignore, .eslintrc.json</files>
  <read_first>
    - /mnt/c/KaoProJ/kaokao/CLAUDE.md (stack decisions — Next.js 14, exact package names; commands: npm run lint)
    - /mnt/c/KaoProJ/kaokao/.gitignore (must include .env.local; verify it is present)
    - /mnt/c/KaoProJ/kaokao/package.json (check if Next.js is already installed)
  </read_first>
  <action>
    Run: npx create-next-app@14 . --typescript --app --no-src-dir --no-tailwind --import-alias "@/*" --use-npm

    Do NOT pass --no-eslint. ESLint must be wired by create-next-app so that npm run lint works. The generated .eslintrc.json and the "lint": "next lint" script in package.json are required.

    If the project already has a package.json with next listed (check before running), skip create-next-app and instead ensure the following packages are installed: next@14, react@18, react-dom@18, typescript@5, @types/node, @types/react, @types/react-dom, eslint, eslint-config-next.

    After scaffolding, confirm the generated config file is next.config.mjs (Next.js 14's default). Do NOT rename it to next.config.ts — TypeScript config files are only supported in Next.js 15+; using .ts causes the config to be silently ignored in Next 14.

    Update next.config.mjs to add:
    - images.remotePatterns: one entry with protocol "https", hostname "*.supabase.co", pathname "/**", port ""
    - Do NOT add i18n, experimental.serverComponentsExternalPackages (add sharp there only in Phase 3)

    Confirm .gitignore contains .env.local. It is already present from the repo init; only append the line if it is missing — do not overwrite the existing .gitignore.
  </action>
  <verify>
    <automated>cd /mnt/c/KaoProJ/kaokao && node -e "const p=require('./package.json'); console.log('next:', p.dependencies.next, 'lint script:', p.scripts.lint)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists next@14.x and react@18.x as dependencies
    - package.json scripts.lint equals "next lint"
    - next.config.mjs (not .ts) exists and exports default config with images.remotePatterns containing hostname "*.supabase.co"
    - .eslintrc.json exists with extends including "next/core-web-vitals"
    - .gitignore contains ".env.local"
    - tsconfig.json has "paths": { "@/*": ["./*"] } so @/lib/utils resolves correctly
  </acceptance_criteria>
  <done>next.config.mjs exists with supabase.co remote pattern; package.json has next@14 and lint script; ESLint is configured; .gitignore covers .env.local</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind CSS with KAOKAO design tokens and initialize shadcn/ui</name>
  <files>tailwind.config.ts, postcss.config.mjs, app/globals.css, components.json, lib/utils.ts, app/layout.tsx, app/page.tsx</files>
  <read_first>
    - /mnt/c/KaoProJ/kaokao/DSprompt.md (exact color palette, typography choices — Quicksand + Nunito, spacing, border radius)
    - /mnt/c/KaoProJ/kaokao/CLAUDE.md (shadcn/ui and Tailwind are the confirmed stack)
    - /mnt/c/KaoProJ/kaokao/spec.md section 6 (non-functional: responsive mobile-first, 375px breakpoint)
  </read_first>
  <action>
    Install Tailwind and utilities: tailwindcss@3, postcss@8, autoprefixer, clsx, tailwind-merge, class-variance-authority, lucide-react.

    Initialize shadcn/ui using the current CLI name: npx shadcn@latest init
    If the command requires interactive input that cannot be suppressed with --defaults, create components.json manually at the project root with: style=default, rsc=true, tsx=true, tailwind.config=tailwind.config.ts, tailwind.css=app/globals.css, aliases.components=@/components, aliases.utils=@/lib/utils.

    Add shadcn components for the root page: npx shadcn@latest add button card
    (Use "shadcn" not "shadcn-ui" — shadcn-ui is deprecated.)

    Configure tailwind.config.ts:
    - content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}", "./lib/**/*.{ts,tsx}"]
    - theme.extend.colors: kaokao — beige: "#E8DCCB", cream: "#F7F3EE", brown: "#A68A64", sage: "#A8C3A0", orange: "#F4B183", "text-dark": "#3D3D3D"
    - theme.extend.fontFamily: sans: ["var(--font-quicksand)", ...defaultTheme.fontFamily.sans], display: ["var(--font-nunito)", ...defaultTheme.fontFamily.sans]
    - theme.extend.screens: xs: "375px"

    In app/globals.css: add @tailwind base, @tailwind components, @tailwind utilities directives. Set CSS variables for shadcn/ui tokens: --background using warm beige #E8DCCB, --foreground using dark text #3D3D3D, --primary using soft brown #A68A64, --radius: 0.75rem.

    In app/layout.tsx: import Quicksand and Nunito from next/font/google. Configure each with subsets: ["latin"]. Apply as CSS variables on the html element: className combining font variables. Import globals.css. Set metadata.title = "KAOKAO" and metadata.description = "Find lost pets near you".

    In app/page.tsx: render a placeholder using shadcn Card and Button. Include an h1 with text "KAOKAO" using the font-display class and a paragraph "Find lost pets near you". Add a Button labeled "Get Started". No database calls, no imports from lib/supabase or lib/prisma.

    In lib/utils.ts: export cn() that calls clsx and then twMerge. Function signature: export function cn(...inputs: ClassValue[]): string.
  </action>
  <verify>
    <automated>cd /mnt/c/KaoProJ/kaokao && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 with no TypeScript or ESLint errors
    - npm run lint exits 0
    - tailwind.config.ts theme.extend.colors contains keys: beige, cream, brown, sage, orange
    - tailwind.config.ts content array includes "./app/**/*.{ts,tsx}" and "./components/**/*.{ts,tsx}"
    - app/globals.css contains "@tailwind base", "@tailwind components", "@tailwind utilities"
    - app/layout.tsx imports both Quicksand and Nunito from "next/font/google"
    - lib/utils.ts exports a function named cn using clsx and tailwind-merge
    - components.json exists at project root with a style field
    - components/ui/button.tsx exists (shadcn added successfully)
  </acceptance_criteria>
  <done>npm run build exits 0; npm run lint exits 0; Tailwind design tokens wired; shadcn/ui Button and Card render on root page; fonts load via next/font</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party packages installed from public registry |
| Developer machine → git repo | .env.local must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local / secrets | mitigate | Confirm .gitignore contains .env.local before first commit |
| T-01-02 | Tampering | npm packages (create-next-app, shadcn, tailwindcss) | mitigate | All packages are well-known with >1M weekly downloads; verify via npmjs.com before install; lock with package-lock.json |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck — verify all installed packages are legitimate before installing: create-next-app (npmjs.com/package/create-next-app), shadcn (npmjs.com/package/shadcn), tailwindcss (npmjs.com/package/tailwindcss), clsx, tailwind-merge, lucide-react |
</threat_model>

<verification>
1. Dev server: `npm run dev &amp; sleep 5 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000` — expect 200
2. Build passes: `npm run build` exits 0
3. Lint passes: `npm run lint` exits 0
4. Tailwind tokens: `grep -v "^\s*//" tailwind.config.ts | grep -c "beige\|cream\|brown\|sage"` — expect >= 4
5. shadcn/ui wired: `test -f components/ui/button.tsx &amp;&amp; echo OK`
6. .env.local gitignored: `grep -F ".env.local" .gitignore` — returns match
</verification>

<success_criteria>
- npm run dev boots and http://localhost:3000 returns 200
- npm run build exits 0 with no errors
- npm run lint exits 0
- KAOKAO palette colors defined in tailwind.config.ts
- Quicksand + Nunito fonts loaded via next/font/google in layout.tsx
- shadcn/ui Button and Card components installed and render on root page
- .env.local is in .gitignore
- next.config.mjs (not .ts) is the active Next.js configuration file
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
